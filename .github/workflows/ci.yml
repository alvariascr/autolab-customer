name: Customer CI

on:
  push:
    branches:
      - main
      - master
      - develop
      - 'codex/**'
      - 'Emilio/**'
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout autolab_core
        uses: actions/checkout@v4
        with:
          repository: BlueZoneCP/autolab-core
          ref: codex/core-architecture-cleanup
          path: autolab_core
          token: ${{ secrets.AUTOLAB_CORE_READ_TOKEN }}

      - name: Link autolab_core for path dependency
        run: ln -s "$GITHUB_WORKSPACE/autolab_core" ../autolab_core

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Check formatting
        run: dart format --output=none --set-exit-if-changed lib test

      - name: Analyze
        run: flutter analyze lib test

      - name: Test
        run: flutter test
